# Shared facts: Distributed systems (cs-5-distributed-systems)

Every card writer reads this file before writing. Reuse these values exactly.

## This course
The source guide's distributed systems, networks, consistency and service discovery pages, section by section, plus the consensus, hashing and retry cards the source assumes you know.
- 20 concept cards plus `_overview.html` (written last by the main agent).
- **Running example / conventions for this course:** The source's own running system: an image-resize service. 100K users, 10K requests/s per region, p99 < 500 ms for images < 1 MB, 99.9% availability; 256 KB average image → 10 conversions/s per core → 1K cores → ~32 × 32-core servers, 40+ with 20% headroom (distributed-systems.md). Reuse it wherever a card needs a system. Shared sourced RTTs (from partial-failure-cap; Azure median RTTs, learn.microsoft.com/en-us/azure/networking/azure-network-latency, 30 days to 2026-07-30): East US→East US 2 8 ms, East US→West US 69 ms, East US→West Europe 83 ms, East US→Japan East 162 ms; fibre ≈ 200,000 km/s; zones in a region: single-digit ms (AWS). Image-service sizing (image-resize-sizing): 40 machines = 78.1% busy; 32 machines = 97.7% busy with 592 ms p99 queue wait (M/M/c model).

## Series facts (shared by every cs-* course)

### The source
- **ljeng/cheat-sheet**, commit `5cedb05` (2026-09-16), https://github.com/ljeng/cheat-sheet. Local copy (read your section in full before writing): `~/Desktop/repos/explainers/tasks/cs-kit/source/`.
- In the card footer, cite the exact source section, e.g. `Source: <a href="https://github.com/ljeng/cheat-sheet/blob/main/coding-algorithms/sorting.md#quicksort">cheat-sheet › Sorting › Quicksort</a>`. For supplemented cards (`[S]`), say "The source heading is empty; this card is supplemented" and cite the references you used instead (CLRS, OSTEP, Hull, DDIA, the paper, the Python docs…).
- The source is terse: mostly problem statements plus one-line solutions, or dense paper notes. **Your job is to teach the idea**, not transcribe. Keep the source's own example or problem as the worked example wherever there is one, so the card is faithful to it.
- When the source is wrong, show the correct thing and add one muted line: "The source's version has X; corrected here." Never silently copy a bug.

### The series (link sibling cards as `../<course-slug>/<card>.html`, using the card list at the end of this section)
cs-0-map · cs-1-data-structures · cs-2-algorithms · cs-3-graphs-dp-math · cs-4-design-foundations · cs-5-distributed-systems · cs-6-data-systems · cs-7-operating-systems · cs-8-probability-discrete · cs-9-probability-continuous · cs-10-financial-math

### The reader
- Preparing for software-engineering, systems-design and research-engineering (quant-adjacent) interviews. Comfortable programming; wants the **why** and a trace they can redo on paper, then a Python snippet they can run.
- Machine: Apple M3 (4 performance + 4 efficiency cores), 24 GB, macOS 15.3. Python **3.10.20** is `python3`; also installed: 3.14.7 (`python3.14`) and free-threaded 3.14.6 (`~/.local/share/uv/python/cpython-3.14.6+freethreaded-macos-aarch64-none/bin/python3.14t`). numpy 2.2.6 under 3.10. **No scipy.**

### Code: Python only
- **Every code example on every card is Python**, including where the source used Java or C++. Rewrite it idiomatically; don't transliterate. Use the standard library first (`heapq`, `collections`, `bisect`, `itertools`, `functools`, `math`, `random`, `threading`, `asyncio`). numpy is OK where it teaches something. No scipy (write `math.erf`-based `norm_cdf` when you need it).
- The snippet must **run under `python3` (3.10)**. Run it, put its real output on the card (a `# →` comment or a small output block), and save the exact snippet to `DIR/code/<card-slug>.py`. The saved file ends with a `if __name__ == "__main__":` demo that prints what the card shows. Link it in the footer: `Run it: <a href="https://github.com/seyonv/explainers/blob/main/<course>/code/<card-slug>.py">code/<card-slug>.py</a>`.
- Keep code short: aim for 8–30 lines on the card. Lines **≤ 72 characters**. Show the core, and leave the full version in `code/`.
- Code block markup (add this CSS to the card's `<style>`, verbatim, and use it for all code):
```css
.code{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:12.5px;line-height:1.5;background:var(--surface);border:1px solid var(--border);border-radius:8px;padding:12px 14px;margin:10px 0;white-space:pre;overflow-x:auto;tab-size:4;color:var(--text)}
.code .k{color:var(--accent);font-weight:600}.code .c{color:var(--muted)}.code .o{color:var(--muted)}
```
  `<pre class="code">` with HTML-escaped content. `.k` = keywords (def, return, for, while, if, class…), `.c` = comments, `.o` = printed output lines. No other highlighting.
- Complexity line on every algorithm or data-structure card: `Time O(…) · Space O(…)` plus one sentence on *why*.

### Colour meanings (same on every card)
- green `--accent`: the answer, the current or active element (pointer, pivot, frontier), the winner/faster option, what's kept
- grey `--faint` / `--surface2`: already processed, visited or discarded; overhead; waiting
- neutral `--text`/`--muted`: data at rest (array cells, nodes, rows)
- red `--red`: ✗, a bug, a violation (overflow, deadlock, lost update), the worst value, a cutoff

### Local measurements (Apple M3, 2026-09-23; `tasks/cs-kit/measure.py`, `race2.py`, `gil.py`; best of 3 unless noted)
Only measurements in this table may be shown as "measured on an M3". Label anything else you time yourself "one run on the author's M3" and keep it light (< 5 s of CPU, no parallel benchmarks).
| What | Result |
|---|---|
| `sorted()` 10⁶ random floats (Timsort), 3.10 | 0.130 s |
| `sorted()` on already-sorted 10⁶ | 0.157 s (includes the inner sort; Timsort on sorted input alone is O(n)) |
| `np.sort` 10⁶ float64, kind="quicksort" (introsort) / "stable" (timsort for floats; radix only for ≤16-bit ints) | 0.031 s / 0.072 s |
| pure-Python insertion sort, n = 5,000 random | 0.828 s (vs `sorted()` 0.00026 s) |
| 1,000 membership tests, n = 10⁵: list / set / dict | 0.232 s / 24.6 µs / 26.5 µs (≈ 9,400× list→set) |
| `for i in range(len(L)): L[i] += 17`, 10⁷ items / numpy `a += 17` | 0.582 s / 0.00286 s (≈ 203×) (the source's 10⁸ numbers: 9.64 s vs 0.188 s) |
| `s = s + str(i) + " "` × 10⁵ / `" ".join(...)` | 0.468 s / 0.0073 s |
| 4 × fib(27), CPU-bound, 3.10: serial / 4 threads / 4 processes | 0.121 s / 0.121 s / 0.032 s |
| same, 3.14.7 (GIL) | 0.086 / 0.082 / 0.022 s |
| same, 3.14.6 free-threaded (no GIL) | 0.045 / 0.017 / 0.013 s |
| 8 × `sleep(0.1)`: serial / 8 threads | 0.835 s / 0.105 s |
| `multiprocessing.Process` start+join (spawn) | 72 ms (median of 5) |
| pipe ping-pong round trip between 2 processes | 13.6 µs |
| `threading.Event` ping-pong round trip between 2 threads | 8.2 µs |
| uncontended `Lock.acquire()+release()` / `with lock:` | 58 ns / 87 ns |
| lost updates, 4 threads × 10⁵ of `v=c; sleep(0); c=v+1`, no lock | 100,096 of 400,000 (3.10); with Lock: 400,000 |
| 4 threads × 10⁶ of `c["n"] += 1`: 3.10 / 3.14 GIL / 3.14t free-threaded | 4,000,000 / 4,000,000 / **1,082,391** |
| 10,000 concurrent `asyncio.sleep(0.1)` | 0.25 s total |
| `getrusage`: 200 × `sleep(1 ms)` voluntary switches / fib(30) (0.129 s) involuntary switches | 200 / 25 |
| `sys.getswitchinterval()` default | 0.005 s |

### Every card in the series (use these exact paths for Related links)
- **cs-1-data-structures**: big-o-amortized, python-toolkit, hash-buckets, sliding-window-counts, sliding-window-median, monotonic-deque, deque-rolling-dp, stack-parsing, monotonic-stack, linked-bucket-list, binary-tree-traversal, fenwick-tree, trie-segment-tree, heaps, fibonacci-heap, lfu-cache, random-pick-structures, stateful-api-read4, edge-case-parsing, testing-your-code
- **cs-2-algorithms**: insertion-sort, quicksort, quickselect, merge-sort, merge-sort-counting, heapsort, radix-sort, sort-lower-bound-stability, index-as-hash, binary-search-on-answer, binary-search-partition, lis-patience, prefix-sum-ordered-set, two-pointers-intervals, kmp, rolling-hash, manacher, suffix-automaton, divide-and-conquer, greedy-wildcard-justify, greedy-candy-patching
- **cs-3-graphs-dp-math**: graph-representations, dfs-flood-fill, bfs-layers, bidirectional-bfs, union-find, toposort, bipartite, eulerian-path, dijkstra, a-star-potentials, bellman-ford-floyd, mst-kruskal-prim, recursion-to-dp, dp-string-matching, dp-palindrome-cuts, dp-grid, dp-state-machine, dp-decode-ways, dp-path-reconstruction, backtracking, bit-arithmetic, newton-sqrt, gcd-number-theory, discrete-math-modular, fibonacci-counting, alias-method, factorial-number-system
- **cs-4-design-foundations**: requirements-scoping, tradeoffs, simplicity, robustness, api-layers, interfaces-vs-abstract, classes-encapsulation, inheritance-polymorphism, ood-practice, back-of-envelope, averages-and-expected-value, slo-arithmetic, algorithm-beats-hardware, compiler-optimizations, for-loop-vectorization, binary-tree-pattern
- **cs-5-distributed-systems**: correlated-failure, image-resize-sizing, image-resize-architecture, partial-failure-cap, backpressure-metrics, network-unreliability, end-to-end-argument, delay-timeouts-congestion, request-path, service-discovery, load-balancing-consumers, eventual-consistency-limits, i-confluence, exactly-once-transfer, quorums-staleness, compaction-xa, consensus-raft, consistent-hashing, sharding-hot-keys, idempotency-retries
- **cs-6-data-systems**: database-indexes, lsm-trees, bigtable-column-families, cache-conscious-structures, cache-patterns, in-memory-db-anticaching, log-structured-memory, mapreduce-model, mapreduce-execution, graph-navigation, query-complexity-classes, semi-naive-datalog, gpu-fair-share, backfill-gang-scheduling, scheduler-as-product, order-book-to-system, aad-tape
- **cs-7-operating-systems**: processes, threads, locks, mutexes, semaphores, monitors, deadlock, livelock, context-switch-how, context-switch-initiation, context-switch-hardware, scheduling, modern-concurrency
- **cs-8-probability-discrete**: counting-inclusion-exclusion, stars-and-bars, rook-placements-bridge, conditional-independence, bayes-table, convolution-discrete, first-step-analysis, markov-chains-martingales, mgf-identify, sigma-coverage-zoo, binomial-beta-binomial, geometric-memoryless, hypergeometric-capture-recapture, negative-binomial, poisson-sum-and-moments, uniform-discrete-continuous
- **cs-9-probability-continuous**: mixed-cdf, pareto-conditional, beta-quantiles, exponential-memoryless, weibull-gamma-function, normal-and-chebyshev, expected-revenue-overbooking, deductibles-and-limits, gini-lorenz, hierarchical-models, joint-table-conditional, order-statistics-min-max, sample-range-uniform, clt-sums, mgf-of-product, coupon-collector, normal-approx-binomial, conditional-on-sum, sample-size
- **cs-10-financial-math**: time-value-of-money, returns-log-returns, bonds-duration, portfolio-mean-variance, sharpe-and-capm, kelly-criterion, random-walks, brownian-motion-gbm, binomial-pricing, black-scholes-greeks, monte-carlo-pricing, ev-games-market-making


## Card list (cs-5-distributed-systems)
| # | File | Title | Group | Source section | Brief |
|---|---|---|---|---|---|
| 1 | correlated-failure.html | Why independent-failure math lies | The reality | [large-scale-design/system-design/distributed-systems.md#the-reality](https://github.com/ljeng/cheat-sheet/blob/main/large-scale-design/system-design/distributed-systems.md#the-reality) | Source: The Reality part 1 (Kingsbury quote; P^N < R demolished by shared power, the same bug, a bad config). Worked: 3 replicas at p=0.01 independent → 10⁻⁶ all-down, vs with a 1% correlated event → ≈ 0.01. Compute. Ask vendors for MTTF/MTTR distributions. |
| 2 | image-resize-sizing.html | Sizing a service: image resize | The reality | [large-scale-design/system-design/distributed-systems.md#the-reality](https://github.com/ljeng/cheat-sheet/blob/main/large-scale-design/system-design/distributed-systems.md#the-reality) | Source: The Reality part 2. Recompute the chain exactly (show each step), plus the SOA 6-item checklist (global/regional, tenancy, guarantees, IAAA, billing/quota, deploys) as a table filled in for this service. |
| 3 | image-resize-architecture.html | Choosing consistency per subsystem | The reality | [large-scale-design/system-design/distributed-systems.md#the-reality](https://github.com/ljeng/cheat-sheet/blob/main/large-scale-design/system-design/distributed-systems.md#the-reality) | Source: The Reality part 3 (Figure 1 and 5 decisions: API tier 99.99% with 3+ servers at 5K RPS; FIFO queue favouring consistency with Paxos failover; auth management plane vs data plane; edge cache favouring availability; usage: local aggregation → Dynamo-style store → MapReduce → billing and cold storage). Draw it as an SVG pipeline, colour-coding C vs A choices. |
| 4 | partial-failure-cap.html | Partial failure and CAP | The reality | [large-scale-design/system-design/distributed-systems.md#engineering](https://github.com/ljeng/cheat-sheet/blob/main/large-scale-design/system-design/distributed-systems.md#engineering) | Source: Engineering part 1 (partial failure; CAP definitions: linearizable, available = every non-failing node responds; Figures 2–3; speed of light). Numbers: RTT across a region ~1 ms vs cross-continent ~70–150 ms (cite a source, e.g. a cloud latency table); a coordinated write pays ≥ 1 RTT. Show a partition scenario with two replicas. |
| 5 | backpressure-metrics.html | Backpressure, percentiles, yield and harvest | The reality | [large-scale-design/system-design/distributed-systems.md#engineering](https://github.com/ljeng/cheat-sheet/blob/main/large-scale-design/system-design/distributed-systems.md#engineering) | Source: Engineering part 2 (backpressure, timeouts, exponential backoff; Figure 4: mean vs percentile latency; uptime vs yield; the 3-shard harvest example: B down → 67% harvest). Worked: a latency sample where mean looks fine but p99 is 10× (compute from a generated lognormal with a fixed seed, label illustrative); fan-out to 100 servers each with 1% slow → 63% of requests slow (compute). |
| 6 | network-unreliability.html | The network is unreliable | The network | [large-scale-design/system-design/networks.md](https://github.com/ljeng/cheat-sheet/blob/main/large-scale-design/system-design/networks.md) | Source: networks.md › Unreliability (the six cases; FLP; ~12 faults/month in a midsize data centre; CENIC >500 partitions, median 2.7 min software / 32 min hardware, hardware p95 3.7 days; Pakistan/YouTube BGP 2008; sharks). A diagram of the six failure cases, all looking identical to the caller. |
| 7 | end-to-end-argument.html | The end-to-end argument | The network | [large-scale-design/system-design/networks.md](https://github.com/ljeng/cheat-sheet/blob/main/large-scale-design/system-design/networks.md) | Source: networks.md › E2E (careful file transfer; ZooKeeper 20 s ephemeral timeout → MTTR; TCP close/SO_LINGER/shutdown-read-0 in C: **rewrite that snippet in Python socket calls**; RST/FIN as fast crash signals). Compute: per-hop error rate 10⁻⁹ over 10 hops on 10 GB → expected undetected errors without an end-to-end check. |
| 8 | delay-timeouts-congestion.html | Delay, timeouts and congestion control | The network | [large-scale-design/system-design/networks.md](https://github.com/ljeng/cheat-sheet/blob/main/large-scale-design/system-design/networks.md) | Source: networks.md › Delay (τ ≤ nP/R + ε, doubled for mesochronous (the source mislabels ε as 'e'); timeout 2d + r; AIMD cwnd += 1/cwnd, halve on loss; QJUMP f, n' = n/f, 802.1Q 0–7, ~35 cycles; EC2 loss <0.5% but episodes in >10% of intervals; TCP vs VoIP). Simulate AIMD for 40 RTTs with a loss every time cwnd > 16 (sawtooth chart, compute). |
| 9 | request-path.html | The path of a request | The network | [large-scale-design/system-design/how-internet-works.md](https://github.com/ljeng/cheat-sheet/blob/main/large-scale-design/system-design/how-internet-works.md) | [S] The source's Routers and Firewalls headings are empty (and its DNS/LB sections are really about service discovery and messaging). Supplement: DNS resolution steps and TTL caching, routing (longest-prefix match with a 3-entry table example; BGP between networks), stateful vs stateless firewalls (a rule table evaluated for 3 packets), L4 vs L7 load balancing (round robin / least connections / consistent hash). A to-scale latency waterfall for one HTTPS request (illustrative, or measure with curl -w on a public site and say so). |
| 10 | service-discovery.html | Service discovery | The network | [large-scale-design/system-design/how-internet-works.md#domain-name-servers](https://github.com/ljeng/cheat-sheet/blob/main/large-scale-design/system-design/how-internet-works.md#domain-name-servers) | Source: how-internet-works › Domain Name Servers (really service discovery): DNS limits, ZooKeeper's two uses, ephemeral nodes, GC pauses → quorum loss, not a cache or pub-sub, the alternatives table, etcd/Consul/Serf, ZK 3.4 vs 3.5. Quorum sizes: 3 nodes tolerate 1, 5 tolerate 2. |
| 11 | load-balancing-consumers.html | Spreading work: competing consumers vs partitions | The network | [large-scale-design/system-design/how-internet-works.md#load-balancers](https://github.com/ljeng/cheat-sheet/blob/main/large-scale-design/system-design/how-internet-works.md#load-balancers) | Source: how-internet-works › Load Balancers (really messaging): competing consumers (AMQP/JMS) vs log-partition assignment; consumers ≤ partitions; head-of-line blocking; shared-memory dynamic partitioning; centralised vs distributed schedulers. Worked: 6 partitions, 4 consumers → 2,2,1,1 assignment; one slow message blocks its whole partition (simulate). |
| 12 | eventual-consistency-limits.html | What eventual consistency can't promise | Consistency | [large-scale-design/system-design/designing-system-certain-constraints.md](https://github.com/ljeng/cheat-sheet/blob/main/large-scale-design/system-design/designing-system-certain-constraints.md) | Source: the first part (4 impossible guarantees; target properties: global validity, transactional availability, convergence, coordination-freedom). A two-replica timeline where two users both get username 'sam'. |
| 13 | i-confluence.html | Invariant confluence | Consistency | [large-scale-design/system-design/designing-system-certain-constraints.md](https://github.com/ljeng/cheat-sheet/blob/main/large-scale-design/system-design/designing-system-certain-constraints.md) | Source: the middle (versioned items, merge ⊔ commutative/associative/idempotent, invariants, Theorem 1; the Alice 10–11 / Bob 11–12 room booking). Table of invariants → I-confluent? (unique id: no; balance ≥ 0 with withdrawals: no; with deposits only: yes; overlap-free bookings: no in general; foreign-key insert: yes, delete: no), each with a 2-replica counterexample. |
| 14 | exactly-once-transfer.html | Money transfer without distributed transactions | Consistency | [large-scale-design/system-design/designing-system-certain-constraints.md](https://github.com/ljeng/cheat-sheet/blob/main/large-scale-design/system-design/designing-system-certain-constraints.md) | Source: the end (Bayou primary commit, log truncation, Manhattan; Example 1 sequence diagram; foreign keys; footnote [^08] is mis-referenced as [^01] in the source). Python: a tiny dedup-by-request-id processor; replay the log twice and show balances unchanged. |
| 15 | quorums-staleness.html | Quorums and quantifying 'eventual' | Consistency | [large-scale-design/system-design/limitations.md](https://github.com/ljeng/cheat-sheet/blob/main/large-scale-design/system-design/limitations.md) | Source: limitations.md part 1 (lag = leader − follower position; Figure 1 n=5 w=3 r=3 with two failed; LWW clock-skew loss; Riak PW/PR; failed write reappearing via read repair; WARS limitations). Compute P(stale read) for n=3, w=1, r=1 when the write has reached k replicas (hypergeometric), and show w + r > n overlap. |
| 16 | compaction-xa.html | Compaction, deletion and XA | Consistency | [large-scale-design/system-design/limitations.md](https://github.com/ljeng/cheat-sheet/blob/main/large-scale-design/system-design/limitations.md) | Source: limitations.md part 2 (LSM/append-only compaction: Datomic, CouchDB, RethinkDB; true deletion and GDPR; XA problems: coordinator SPOF, stateful app servers, lowest common denominator, failure amplification). Worked: 2PC message count 4(n) for n participants and the blocking window when the coordinator dies after PREPARE. |
| 17 | consensus-raft.html | Consensus with Raft | Consistency | [large-scale-design/system-design/distributed-systems.md](https://github.com/ljeng/cheat-sheet/blob/main/large-scale-design/system-design/distributed-systems.md) | [S] The source names Paxos (image-service queue failover) and ZooKeeper/etcd but never explains consensus. Teach Raft: terms, randomized election timeouts (150–300 ms), AppendEntries, commit on majority, the safety argument (two majorities intersect). A 5-node timeline: leader crash, election, one follower lagging. Cite the Raft paper. |
| 18 | consistent-hashing.html | Consistent hashing | Scaling data | [large-scale-design/system-design/distributed-hash-table-system.md](https://github.com/ljeng/cheat-sheet/blob/main/large-scale-design/system-design/distributed-hash-table-system.md) | [S] distributed-hash-table-system.md is actually about Bigtable column families (covered in cs-6). Teach hash mod N vs a hash ring: adding a 5th node to 4 moves 80% of keys with mod vs ~20% with a ring (compute over 100k keys with hashlib); virtual nodes and load spread (std dev of load with 1 vs 100 vnodes per node, computed); Dynamo/Chord in one line each. |
| 19 | sharding-hot-keys.html | Sharding and hot keys | Scaling data | [large-scale-design/system-design/distributed-systems.md](https://github.com/ljeng/cheat-sheet/blob/main/large-scale-design/system-design/distributed-systems.md) | [S] Implied everywhere in the source (MapReduce hash mod R, H-Store partitions) but never covered. Range vs hash with the queries each supports; a Zipf(s=1.1) key distribution over 16 shards: share of load on the hottest shard (compute with a fixed seed); key salting with 8 suffixes and the read fan-out cost. |
| 20 | idempotency-retries.html | Idempotency and retries | Scaling data | [large-scale-design/system-design/networks.md](https://github.com/ljeng/cheat-sheet/blob/main/large-scale-design/system-design/networks.md) | [S] The source mentions timeouts and exponential backoff (distributed-systems, networks) and the money-transfer needs exactly-once, but never covers retries. Backoff schedule base 100 ms ×2 capped at 10 s with full jitter (compute the first 6 delays, show expected values); retry amplification 3 layers × 3 retries = 27× (compute (r+1)^layers = 64 with 3 retries); idempotency key table on the server; which HTTP methods are idempotent. |
